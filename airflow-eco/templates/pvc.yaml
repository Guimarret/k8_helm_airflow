apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airflow-pvc1
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
